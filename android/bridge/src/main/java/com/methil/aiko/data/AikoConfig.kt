package com.methil.aiko.bridge

object AikoConfig {
    // Le Backend Go sert de point d'entrée unique (Proxy)
    val BASE_URL = BuildConfig.BASE_URL
    val DEFAULT_MODEL = BuildConfig.DEFAULT_MODEL
}